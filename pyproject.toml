[project]
name = "example-cli"
version = "0.1.0"
description = ""
authors = [
    {name = "Diego Escobar",email = "luisdiego.escobar@salud.gob.sv"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click (>=8.3.3,<9.0.0)",
    "questionary (>=2.1.1,<3.0.0)"
]

[project.scripts]
fastcli = "example_cli.main:group_root"

[tool.poetry]
packages = [{include = "example_cli", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
fastcli = "example_cli.main:group_root"